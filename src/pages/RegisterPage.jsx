import RegisterForm from '../features/auth/components/RegisterForm';

/**
 * RegisterPage – renders the RegisterForm inside the AuthLayout.
 */
const RegisterPage = () => {
  return <RegisterForm />;
};

export default RegisterPage;
