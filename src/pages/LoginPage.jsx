import LoginForm from '../features/auth/components/LoginForm';

/**
 * LoginPage – renders the LoginForm inside the AuthLayout.
 */
const LoginPage = () => {
  return <LoginForm />;
};

export default LoginPage;
